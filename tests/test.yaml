distribution:
    source: debian

playbook:
    -  name: base packages
       tasks:
           - name: install vim
             apt:
                 name: vim
                 state: present
             become: true

image:
    filename: test-image.img
    table: gpt
    size: 960MiB
    partitions:
        - label: system
          group: vg_sys
          size: 950MiB
          flags:
              - boot
              - primary
              - lvm
    volumes:
        - label: lv_root
          group: vg_sys
          size: 840MiB
          where: /
        - label: lv_data
          group: vg_sys
          size: 100MiB
          where: /var
