distribution:
    architecture: amd64

playbook:
    -  name: boot packages
       tasks:
           - name: install boot-loader
             apt:
                 name: grub-efi-amd64-bin
                 state: present
           - name: install kernel
             apt:
                 name: linux-image-amd64
                 state: present
